rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Business media files
    match /businesses/{businessId}/media/{fileName} {
      // Allow public read for posted content
      allow read: if true;
      // Only Cloud Functions can write
      allow write: if false;
    }
  }
}
